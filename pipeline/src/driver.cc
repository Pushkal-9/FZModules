// get all modules compiled at least w/ low level C++ api

// one compiled work on C and C++ api interface w/ team

// python interface once we're happy with C/C++ api (ctypes?) (SWIG?)

// libpressio integration? ask robert/song/PIs

// sz3 integration? add in these modules -- fit this to SZ3 or SZ3 to this?